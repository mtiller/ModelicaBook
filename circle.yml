machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker pull mtiller/book-builder:om-1.12.0

test:
  override:
    - docker run -v .:/opt/MBE/ModelicaBook -i -t mtiller/book-builder make specs
    - docker run -v .:/opt/MBE/ModelicaBook -i -t mtiller/book-builder make results
    - docker run -v .:/opt/MBE/ModelicaBook -i -t mtiller/book-builder make dirhtml
