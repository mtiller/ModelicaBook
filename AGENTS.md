# Worktrees

Each change should be performed in a separate worktree.  Use
the `.worktrees` directory here to hold worktrees.  Name
the worktree based on what it does.

# Main branch

For the time being, all PRs should target the `refactor`
branch.
